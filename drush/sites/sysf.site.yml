prd:
  root: /app/docroot
  uri: https://sysf.one
  host: sysf.one
  user: www-data
  paths:
    drush-script: /app/vendor/bin/drush

stg:
  root: /app/docroot
  uri: https://stg.env.sys.sysf.one
  host: stg.env.sys.sysf.one
  user: www-data
  paths:
    drush-script: /app/vendor/bin/drush

dev:
  root: /app/docroot
  uri: https://dev.env.sys.sysf.one
  host: dev.env.sys.sysf.one
  user: www-data
  paths:
    drush-script: /app/vendor/bin/drush

ide1:
  root: /app/docroot
  uri: https://ide1.env.sys.sysf.one
  host: ide1.env.sys.sysf.one
  user: www-data
  paths:
    drush-script: /app/vendor/bin/drush
